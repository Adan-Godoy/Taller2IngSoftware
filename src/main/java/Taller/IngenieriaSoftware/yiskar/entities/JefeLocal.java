package Taller.IngenieriaSoftware.yiskar.entities;

public class JefeLocal extends Persona{

    /**
     * Método constructor del objeto JefeLocal.
     * @param nombre Nombre del jefe de local.
     * @param edad Edad del jefe de local.
     * @param email Correo del jefe de local.
     * @param contrasenia Contraseña del jefe de local.
     */
    public JefeLocal(String nombre, int edad, String email, String contrasenia) {
        super(nombre,edad,email,contrasenia);

    }
}
